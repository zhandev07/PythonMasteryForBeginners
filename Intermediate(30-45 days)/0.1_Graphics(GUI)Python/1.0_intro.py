import tkinter as tk

# Step 1: Create a main window (root window)
root = tk.Tk()

# Step 2: Set window title
root.title("My First GUI Application")

# Step 3 Set windoe size (width x height)
root.geometry("400x300")

# Step 4: Keep the window running (Mainloop)

root.mainloop()